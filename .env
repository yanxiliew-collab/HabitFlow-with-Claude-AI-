# .env ─ 環境變數設定檔（KEY=VALUE，不需要引號）
# 不進 git（已加入 .gitignore），可安全存放機密資訊

# VITE_API_URL：Vite 在 build 時讀取，寫死進 JS bundle 告訴 React 要打哪個 API
# Vite 只讀取 VITE_ 開頭的變數（安全設計，避免把機密洩漏進前端）
# VMware 用戶改成 VM IP
VITE_API_URL=http://localhost:8000
